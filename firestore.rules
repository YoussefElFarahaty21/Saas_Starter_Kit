rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through the Express backend (Firebase Admin SDK).
    // Client apps must not read/write Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
